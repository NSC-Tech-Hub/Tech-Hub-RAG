- [ ] @oliverwarlick Test the regex parser
- [ ] @mroliverw Setup the automation webhook

#### 2. Write a 15-line Local Python Parser
Write a simple script that reads the markdown file, extracts the tasks using regex, and prints them.

Use Python's built-in re module to look for the pattern: - \[ \] @(\S+) (.+)

Loop through the matches and print them to the terminal to verify your regex works.

#### 3. Define the Webhook Schema
Identify what fields our automation platform (Zapier/Make) will need to send to GitHub when a new file is created. We will need:

file_path: Where the markdown file lives in our repo (e.g., /meeting-notes/notes-date.md)

file_content: The clean markdown text generated by the Slack AI step.

commit_message: A standardized message (e.g., docs: add meeting notes [date])


Definition of Done (DoD)
[ ] A prototype_notes.md file exists in the repo workspace.

[ ] A local Python script successfully parses that specific file format and outputs the matches to the console.

[ ] A documented JSON schema mapping out the fields needed for our inbound Slack/Zapier webhook is saved in the development notes.


*Don't worry about connecting Slack or Zapier yet! We just want to make sure we can parse the markdown locally first.